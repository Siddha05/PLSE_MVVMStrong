<Window x:Class="PLSE_MVVMStrong.View.Expertises"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodel="clr-namespace:PLSE_MVVMStrong.ViewModel"
        xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
        xmlns:local="clr-namespace:PLSE_MVVMStrong.View"
        mc:Ignorable="d"
        Title="Expertises" Width="1980" WindowStartupLocation="CenterScreen" WindowState="Maximized" ResizeMode="CanMinimize">
    <Window.DataContext>
        <viewmodel:ExpertisesVM/>
    </Window.DataContext>
    <Window.Resources>
        <viewmodel:ExpertiseLinkConverter x:Key="ExpLinkConv"/>
        <BooleanToVisibilityConverter x:Key="BVConv"/>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3.5*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <DataGrid Margin="2" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" CanUserResizeRows="False" Background="White"
                  ItemsSource="{Binding Path=ExpertiseList}" GridLinesVisibility="None" BorderBrush="#FF0097FF">
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="SeparatorBrush" Value="Black"/>
                    <Setter Property="Height" Value="40"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="FontStyle" Value="Italic"/>
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    <Setter Property="FontSize" Value="{StaticResource HeaderFontSize}"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type DataGridColumnHeader}">
                                <Border Padding="0,3" BorderThickness="1">
                                    <Border.BorderBrush>
                                        <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                                            <GradientStop Offset="0" Color="#A5A5A5" />
                                            <GradientStop Offset="1" Color="#4B4B4B" />
                                        </LinearGradientBrush>
                                    </Border.BorderBrush>
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                                            <GradientStop Offset="0" Color="#FF7BCEF5" />
                                            <GradientStop Offset="1" Color="#FF0097FF" />
                                        </LinearGradientBrush>
                                    </Border.Background>
                                    <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.RowStyle>
                <Style TargetType="{x:Type DataGridRow}">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type DataGridRow}">
                                <Border x:Name="brd" BorderThickness="1" BorderBrush="Blue" Margin="1,2">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Color="#FFEDF1F3" Offset="0"/>
                                            <GradientStop Color="#FFC9DCFD" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                    <SelectiveScrollingGrid>
                                        <SelectiveScrollingGrid.RowDefinitions>
                                            <RowDefinition/>
                                            <RowDefinition Height="auto"/>
                                        </SelectiveScrollingGrid.RowDefinitions>
                                        <SelectiveScrollingGrid.ColumnDefinitions>
                                            <ColumnDefinition Width="auto"/>
                                            <ColumnDefinition/>
                                        </SelectiveScrollingGrid.ColumnDefinitions>
                                        <DataGridCellsPresenter Grid.Column="1" ItemsPanel="{TemplateBinding ItemsPanel}" 
                                                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                        <DataGridDetailsPresenter Name="dp" Grid.Row="1" Grid.Column="1" Visibility="{Binding ElementName=tb, Path=IsChecked, Converter={StaticResource BVConv}}"
                                                                 SelectiveScrollingGrid.SelectiveScrollingOrientation="{Binding AreRowDetailsFrozen, ConverterParameter={x:Static SelectiveScrollingOrientation.Vertical}, Converter={x:Static DataGrid.RowDetailsScrollingConverter}, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}" >
                                        </DataGridDetailsPresenter>
                                        <ToggleButton Name="tb" Grid.RowSpan="2" Style="{StaticResource ArrowToggle}" IsChecked="False" Height="25" Margin="5,0,0,0" 
                                                      Foreground="#FF6FD809"/>
                                    </SelectiveScrollingGrid>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="true">
                                        <Setter TargetName="brd" Property="Background" Value="#FF83E5F5"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="true">
                                        <Setter TargetName="brd" Property="Background" Value="#FF1285F7"/>
                                    </Trigger>
                                    <DataTrigger Binding="{Binding ElementName=tb, Path=IsChecked}" Value="true">
                                        <DataTrigger.EnterActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetName="dp" Storyboard.TargetProperty="Height" From="0" To="120" Duration="0:0:0.3"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </DataTrigger.EnterActions>
                                        <DataTrigger.ExitActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetName="dp" Storyboard.TargetProperty="Height" To="0" Duration="0:0:0.3"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </DataTrigger.ExitActions>
                                    </DataTrigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridCell">
                                <ContentPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </DataGrid.CellStyle>
            <DataGrid.RowDetailsTemplate>
                <DataTemplate DataType="ResEx">
                    <Grid HorizontalAlignment="Center">
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition/>
                            <RowDefinition/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition/>
                            <ColumnDefinition Width="30"/>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Срок" Grid.Row="2"/>
                        <TextBlock Grid.Column="1" Grid.Row="2" Text="{Binding Path=Expertise.TimeLimit}" HorizontalAlignment="Left"/>
                        <TextBlock Grid.Row="1" Text="Дата сдачи"/>
                        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Path=Expertise.EndDate, StringFormat=d, ConverterCulture='ru-Ru', TargetNullValue=-}"/>
                        <TextBlock Text="Тип"/>
                        <TextBlock Text="{Binding Path=Expertise.ExpertiseType}" HorizontalAlignment="Left" Grid.Column="1"/>
                        <TextBlock Text="В работе" Grid.Row="3"/>
                        <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding Path=Expertise.Inwork}" HorizontalAlignment="Left"/>

                        <TextBlock Grid.Row="1" Grid.Column="3" Text="Связанные экспертизы" Margin="20,0"/>
                        <TextBlock Grid.Row="1" Grid.Column="4" Margin="20,0" Text="{Binding Path=Resolution.Expertisies.Count, Converter={StaticResource ExpLinkConv}}" HorizontalAlignment="Left"/>
                        <TextBlock Grid.Row="2" Grid.Column="3" Text="Вопросы" Margin="20,0"/>
                        <TextBlock Grid.Row="2" Grid.Column="4" Text="{Binding Path=Resolution.Questions.Questions.Count}" HorizontalAlignment="Left"
                                       ToolTip="{Binding Path=Resolution.QeustionsString}" Margin="20,0"/>
                        
                    </Grid>
                </DataTemplate>
            </DataGrid.RowDetailsTemplate>
            <DataGrid.Columns>
                <DataGridTemplateColumn Width="90" Header="Номер">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate DataType="ResEx">
                            <TextBlock HorizontalAlignment="Center" Text="{Binding Path=Expertise.Number}" FontSize="18" VerticalAlignment="Center"/>
                            <DataTemplate.Triggers>
                                
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Width="*" Header="Эксперт">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Style="{x:Null}" Text="{Binding Path=Expertise.Expert}" FontSize="18" TextWrapping="Wrap" TextAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Width="110" Binding="{Binding Path=Expertise.StartDate, Mode=OneWay, StringFormat=d, ConverterCulture='ru-RU'}" Header="Начата">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="{x:Type TextBlock}">
                            <Setter Property="FontSize" Value="18"/>
                            <Setter Property="TextAlignment" Value="Center"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Width="130" Binding="{Binding Path=Expertise.Remain, Mode=OneWay}" Header="Осталось">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="{x:Type TextBlock}">
                            <Setter Property="FontSize" Value="18"/>
                            <Setter Property="TextAlignment" Value="Center"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTemplateColumn Width="*" Header="Заказчик">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate DataType="Expertise">
                            <TextBlock Style="{x:Null}" Text="{Binding Path=Resolution.Customer.Requisite}" FontSize="18" VerticalAlignment="Center" TextAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <ContextMenu.Template>
                        <ControlTemplate TargetType="{x:Type ContextMenu}">
                            <Border Background="White" BorderBrush="Black" BorderThickness="1" Padding="2">
                                <ContentPresenter/>
                            </Border>
                        </ControlTemplate>
                    </ContextMenu.Template>
                    <MenuItem Header="Создать">
                        <MenuItem Header="1"/>
                        <MenuItem Header="2"/>
                    </MenuItem>
                </ContextMenu>
            </DataGrid.ContextMenu>
        </DataGrid>
        <StatusBar Grid.Row="1" Grid.ColumnSpan="2" Height="30" Background="#FF6D6DFB" TextBlock.Foreground="White" 
                   TextBlock.FontSize="{StaticResource UIFontSize}">
            <StatusBar.ItemsPanel>
                <ItemsPanelTemplate>
                    <Grid HorizontalAlignment="Right">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="auto"/>
                        </Grid.ColumnDefinitions>
                    </Grid>
                </ItemsPanelTemplate>
            </StatusBar.ItemsPanel>
            <StatusBarItem>
                <TextBlock Style="{x:Null}" Margin="0,0,5,0" Text="{Binding Path=ExpertiseList.Count, StringFormat=Экспертиз: {0}}"/>
            </StatusBarItem>
            <StatusBarItem Grid.Column="1">
                <TextBlock Style="{x:Null}" Margin="5,0,5,0" Text="{Binding Path=ExpiredExpertise, StringFormat=Просроченных: {0}}"/>
            </StatusBarItem>
            <StatusBarItem Grid.Column="2">
                <TextBlock Style="{x:Null}" Margin="5,0,15,0" Text="{Binding Path=AttentionExpertise, StringFormat=Требующих внимания: {0}}"/>
            </StatusBarItem>
        </StatusBar>
        <Grid Grid.Column="1" Margin="0,30">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <TextBlock Text="Эксперт"/>
            <ComboBox Grid.Column="1" Style="{StaticResource UIComboBox}" ItemsSource="{Binding Path=ExpertList}" DisplayMemberPath="Employee.Fio"
                      SelectedItem="{Binding Path=QEmployee}"/>
            <TextBlock Grid.Row="1" Text="Статус"/>
            <ComboBox Grid.Column="1" Grid.Row="1" ItemsSource="{Binding Path=ExpertiseStatus}" 
                      SelectedItem="{Binding Path=QExpertiseStatus}">
                <ComboBox.Style>
                    <Style TargetType="{x:Type ComboBox}">
                        <Setter Property="FontSize" Value="{StaticResource UIFontSize}"/>
                        <Setter Property="SnapsToDevicePixels" Value="True"/>
                        <Setter Property="Margin" Value="5"/>
                        <Setter Property="ItemsPanel">
                            <Setter.Value>
                                <ItemsPanelTemplate>
                                    <VirtualizingStackPanel IsVirtualizing ="True"/>
                                </ItemsPanelTemplate>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ComboBox}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition/>
                                            <ColumnDefinition Width="40"/>
                                        </Grid.ColumnDefinitions>
                                        <ToggleButton Grid.Column="1">
                                            <ToggleButton.Template>
                                                <ControlTemplate TargetType="{x:Type ToggleButton}">
                                                    <Border CornerRadius="3" BorderBrush="{TemplateBinding BorderBrush}">
                                                        <Path x:Name="Arrow" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                              Data="M -5 0 L 2 7 L 9 0 Z" Fill="#FF23A5CF"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </ToggleButton.Template>
                                        </ToggleButton>
                                    </Grid>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ComboBox.Style>
            </ComboBox>
            <TextBlock Grid.Row="2" Text="Тип"/>
            <ComboBox Grid.Column="1" Grid.Row="2" Style="{StaticResource UIComboBox}" ItemsSource="{Binding Path=ExpertiseTypes}"
                      SelectedItem="{Binding Path=QExpertiseType}"/>
            <Expander Grid.Row="3" Grid.ColumnSpan="2" FontSize="{StaticResource UIFontSize}" FontWeight="Bold" Header="Период начала"
                      Margin="5" BorderThickness="2" BorderBrush="#FF0097FF">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="2,3,2,7" Height="40">
                    <DatePicker Name="dpSStartDate" FontSize="16"/>
                    <Line Stroke="Black" X1="0" Y1="17" X2="15" Y2="17" Margin="5,0" StrokeThickness="2"/>
                    <DatePicker FontSize="16" DisplayDateStart="2011-11-11"/>
                </StackPanel>
            </Expander>
            <Expander Grid.Row="4" Grid.ColumnSpan="2" FontSize="{StaticResource UIFontSize}" FontWeight="Bold" Header="Период сдачи"
                      Margin="5" BorderBrush="#FF0097FF" BorderThickness="2">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="2,3,2,7" Height="40">
                    <DatePicker/>
                    <Line Stroke="Black" X1="0" Y1="17" X2="15" Y2="17" Margin="5,0" StrokeThickness="2"/>
                    <DatePicker/>
                </StackPanel>
            </Expander>
            <Button Grid.Row="20" Grid.ColumnSpan="2" Style="{StaticResource BaseUIButton}" Content="Поиск" Foreground="Blue" Margin="5, 20" 
                    Command="{Binding Path=Find}"/>
        </Grid>
    </Grid>
</Window>
